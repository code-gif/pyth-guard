# Normalise to LF in the repository and on checkout.
#
# `aiken fmt --check` and `ruff` both compare exact bytes, so a CRLF checkout on
# one machine and an LF checkout on another turns a clean tree into a CI
# failure that reproduces nowhere.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gz  binary
